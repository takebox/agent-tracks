# Autoreview Track

- Canonical source: `takebox/agent-tracks`, under `tracks/autoreview`.
- Before editing any copy, fast-forward a checkout of `takebox/agent-tracks` from `origin/main`.
- Make and validate shared changes in canonical `tracks/autoreview` first, then sync the complete directory into downstream repos.
- Never create repo-local behavior variants; downstream differences belong in repo-level validation, not the track.
